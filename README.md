# FlowScore 🎯
**Alternative credit scoring for young people with irregular incomes.**

Built for SkillBoost Lab Hackathon 2026 — track: *Young People & Financial Inclusion*.

---

## What is FlowScore?
FlowScore assigns a 0–100 financial risk score to individuals based on **cash flow patterns** rather than traditional credit history. It targets young Italians with precarious, discontinuous incomes who are structurally excluded from standard banking products.

Standard banking products are designed for stable incomes. FlowScore evaluates what actually matters for this demographic: income volatility, liquidity buffers, fixed cost exposure, and resilience to financial shocks.

---

## Architecture

| Layer | Tool | Description |
|---|---|---|
| Risk Model | R (GLM) | Logistic regression on 6 cash flow variables → FlowScore 0–100 |
| Shock Simulation | C (RK4) | ODE-based debt trajectory under 3 income shock scenarios |
| Demo Pipeline | Python | Reads both outputs, computes fragility index, visualizes results |

The three components communicate via CSV — no cross-language dependencies.

---

## Repo Structure

```
flowscore/
├── data/
│   ├── generate_profiles.py       # generates synthetic user profiles
│   └── profiles.csv               # 100 synthetic profiles
├── model/
│   ├── credit_model.R             # GLM logistic regression (R)
│   └── scores_output.csv          # exported FlowScores
├── simulation/
│   ├── shock_model.c              # RK4 ODE debt simulation (C)
│   ├── Makefile
│   └── simulation_output.csv      # generated by ./shock_model
├── demo/
│   └── flowscore_demo.py          # main demo script
├── presentation/
│   └── slides.pptx
└── README.md
```

---

## Input Variables

| Variable | Description |
|---|---|
| `income_volatility` | Std dev of monthly income / mean income |
| `fixed_cost_ratio` | Fixed expenses / total income |
| `liquidity_buffer` | Savings / avg monthly expenses (months of cover) |
| `bnpl_exposure` | Active BNPL/revolving credit as % of income |
| `income_trend` | Linear trend of income over 6 months |
| `employment_type` | stable / freelance / gig / NEET (encoded 0–3) |

---

## Quick Start

```bash
# 1. Generate synthetic profiles
python data/generate_profiles.py

# 2. Run R model → produces model/scores_output.csv
Rscript model/credit_model.R

# 3. Build and run C simulation → produces simulation/simulation_output.csv
cd simulation && make && ./shock_model 1

# 4. Run demo for a specific user profile
python demo/flowscore_demo.py --user 42
```

---

## Team

| Name | Background | Role |
|---|---|---|
| Chris | MSc AI, University of Trieste | Python pipeline, demo, repo |
| [Name] | MSc Actuarial Science, BSc Mathematics | R risk model |
| [Name] | MSc Physics, ICTP Trieste | C shock simulation |

---

## Status
🚧 Work in progress — Hackathon: March 13–17, 2026
